<div class="col-lg-12 col-xl-5 mt-4 mt-xl-0">
    <div class="single-form-card h-100">
        <div class="card-main">
            <h6 class="mb-3"><i class="fas fa-lock color-base"></i> {{ __('How it works') }}</h6>
            <ul class="list-unstyled small mb-0">
                <li class="d-flex gap-2 mb-3">
                    <span class="badge bg-light text-primary">1</span>
                    <div>
                        <strong>{{ __('Pick a corridor') }}</strong>
                        <div class="text-muted">{{ __('Choose the country you are sending to. The rate locks for 15 minutes.') }}</div>
                    </div>
                </li>
                <li class="d-flex gap-2 mb-3">
                    <span class="badge bg-light text-primary">2</span>
                    <div>
                        <strong>{{ __('Review your quote') }}</strong>
                        <div class="text-muted">{{ __('See exactly how much arrives, the fee, and our exchange rate before you pay.') }}</div>
                    </div>
                </li>
                <li class="d-flex gap-2 mb-3">
                    <span class="badge bg-light text-primary">3</span>
                    <div>
                        <strong>{{ __('Confirm & track') }}</strong>
                        <div class="text-muted">{{ __('We debit your wallet, route the payout, and notify both sides at every step.') }}</div>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</div>
